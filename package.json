{
  "devDependencies": {
    "@types/node": "^20.14.8",
    "typescript": "^5.5.2"
  },
  "name": "voice-bot",
  "version": "1.0.0",
  "main": "dist/index.js",
  "type": "commonjs",
  "scripts": {
    "start:deploy": "node dist/deploy-commands.js && node dist/bot.js",
    "start": "node dist/bot.js",
    "deploy": "node dist/deploy-commands.js",
    "build": "npx tsc",
    "build:watch": "npx tsc --watch",
    "build:clean": "rm -rf dist && npx tsc",
    "build:watch:clean": "rm -rf dist && npx tsc"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@discordjs/opus": "^0.9.0",
    "@discordjs/voice": "^0.17.0",
    "discord-player": "^6.6.10",
    "discord.js": "^14.15.3",
    "dotenv": "^16.4.5",
    "libsodium-wrappers": "^0.7.13"
  }
}
